apiVersion: v1
kind: Service
metadata:
  name: load-harness
  namespace: applications
  labels:
    app: load-harness
    managed-by: flux
spec:
  type: ClusterIP
  selector:
    app: load-harness
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
