apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - podmonitor.yaml
  # The app's own Grafana dashboards travel with it.
  - ../../../applications/load-harness/monitoring
