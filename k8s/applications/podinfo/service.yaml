apiVersion: v1
kind: Service
metadata:
  name: podinfo
  namespace: applications
  labels:
    app: podinfo
    managed-by: flux
spec:
  type: ClusterIP
  selector:
    app: podinfo
  ports:
    - port: 80
      targetPort: 9898
      protocol: TCP
      name: http
