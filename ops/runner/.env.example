# GitHub Actions Runner Configuration
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#

# Required: GitHub token (PAT with 'repo' scope or runner registration token)
GITHUB_TOKEN=ghp_your_token_here

# Optional: Runner name (default: docker-runner)
RUNNER_NAME=docker-runner

# Optional: Runner labels (default: self-hosted,Linux,X64,docker)
RUNNER_LABELS=self-hosted,Linux,X64,docker

# Optional: Repository (default: your-org/infra-fleet)
GITHUB_REPOSITORY=your-org/infra-fleet

# Optional: PAT for runner de-registration on shutdown
# If set, the runner will cleanly remove itself from GitHub when stopped
# GITHUB_PAT=ghp_your_pat_here
